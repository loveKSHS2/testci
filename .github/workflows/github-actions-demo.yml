name: centOS python3.6.8
on: [push]
jobs:
 build:
  runs-on: ubuntu-22.04
  steps:
  - uses: actions/checkout@v2
  - name: Set Up Python 3.6.8
    uses: actions/setup-python@v1
    with:
      python-version: 3.6.8
  - name: Install dependencies
    run: |
     make install 
